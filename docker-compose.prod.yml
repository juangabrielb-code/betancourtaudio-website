# ================================
# Docker Compose - Producción
# Betancourt Audio
# ================================

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: betancourt-audio-prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped
    # Recursos limitados (opcional)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '1'
    #       memory: 512M
    #     reservations:
    #       cpus: '0.5'
    #       memory: 256M
